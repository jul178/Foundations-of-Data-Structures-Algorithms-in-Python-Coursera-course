class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        map = {}
        for s in strs:
            chars = ''.join(sorted(s))
            if chars not in map:
                map[chars] = [s]
            else:
                map[chars].append(s)
        return list(map.values())
