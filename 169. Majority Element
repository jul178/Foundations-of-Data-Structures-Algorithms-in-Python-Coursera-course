class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        half = len(nums) / 2
        freq = {}
        for n in nums:
            if n in freq: 
                freq[n] += 1
            else:
                freq[n] = 1
        for n, val in freq.items():
            if val > half:
                return n
