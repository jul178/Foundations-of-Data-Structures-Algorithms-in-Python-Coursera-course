class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        map = {}
        for n in nums:
            if n in map:
                return True
            map[n] = 1
        return False

        # return len(nums) != len(set(nums))
